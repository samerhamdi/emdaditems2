Fresh rebuild from scratch.

Products: 302
Own row images: 299
Rows with duplicate anchored images where latest image was used: 4
Rows without own image where previous image was repeated: 3

Important mapping rule used:
- Product image is taken from the image anchored on the same Excel row.
- If more than one image is anchored to a row, the latest/topmost inserted image is used.
- If no image is anchored to a row, the previous item image is repeated.
